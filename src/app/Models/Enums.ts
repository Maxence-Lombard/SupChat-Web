export enum visibility {
  "public" = "Public",
  "private" = "Private",
}

export enum status {
  "online" = "Online",
  "away" = "Away",
  "offline" = "Offline",
}

export enum theme {
  "light" = "Light",
  "dark" = "Dark",
}

export enum attachmentType {
  "image" = "Image",
  "video" = "Video",
  "audio" = "Audio",
  "file" = "File",
  "link" = "Link",
  "profilePicture" = "ProfilePicture",
}

export enum types {
  INFO = 0,
  WARNING = 1,
  ERROR = 2,
}

export enum roles {
  "owner" = "Owner",
  "admin" = "Admin",
  "editor" = "Editor",
  "viewer" = "Viewer",
  "guest" = "Guest",
  "custom" = "Custom",
}
