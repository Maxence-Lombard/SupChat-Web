export enum visibility {
    "public" = "Public",
    "private" = "Private",
}

export enum status {
    "Online" = 0,
    "Away" = 1,
    "Offline" = 2,
}

export enum theme {
    "light" = 0,
    "dark" = 1,
}